export default {
  render() {
    return '<section class="card readonly-card" aria-labelledby="sample-read-only-heading">' +
      '<h2 id="sample-read-only-heading">Read-only example</h2>' +
      '<div class="cb"><p>This sample confirms that registered tools can supply safe, read-only content.</p></div>' +
      '</section>' +
      '<p class="summary">A tool with no input keeps the same shell and simply drops the run history drawer.</p>';
  }
};
